# 🧠 AI Checker 免费在线工具 - Dechecker

**Dechecker 的 AI 检测器和检测工具**

---

## 🌐 语言切换 / Language Switch

- [English](README.md) | [中文](README.zh-CN.md)

---

## 🔍 概述

**Dechecker** 是一个强大的 **[AI 检测器](https://dechecker.ai)** 工具，旨在检测文本是由人类撰写还是由 **ChatGPT**、**GPT-5**、**Claude**、**Gemini** 或 **LLaMA** 等 AI 模型生成。
它提供**句子级别的 AI 检测**，高亮显示 AI 撰写的部分，并帮助用户**优化文本的真实性和原创性**。

本仓库包含 Dechecker **AI 检测器**的**后端 Python 项目**，提供用于文本检测和分析的 RESTful API。
您可以将其集成到您的 Web 应用、Chrome 扩展或内部审核工具中。

👉 产品参考： [https://dechecker.ai](https://dechecker.ai)

---

## ⚙️ 核心功能

* **AI 检测** – 识别文本是 AI 生成还是人类撰写，并提供置信度分数。
* **句子级别分析** – 高亮显示可能由 AI 生成的特定句子。
* **内容真实性评分** – 获取表示 AI 作者概率的数值分数。
* **优化建议** – （可选）重写或优化 AI 撰写的内容，使其听起来更人性化和自然。
* **多场景应用** – 适用于学术论文、SEO 内容、博客、社交媒体帖子、论文和文案写作。
* **快速轻量级 API** – 使用 FastAPI/Flask 构建，便于部署和集成。

---

## 🧩 技术栈

* **编程语言：** Python 3.10+
* **框架：** FastAPI（或 Flask）
* **模型后端：** Hugging Face Transformers、PyTorch
* **AI 检测模型：** 微调的 RoBERTa（示例）
* **环境：** 支持 Docker，REST API
* **许可证：** MIT

---

## 🚀 快速开始

### 1️⃣ 克隆仓库

```bash
git clone https://github.com/yourusername/dechecker-ai-checker-backend.git
cd dechecker-ai-checker-backend
```

### 2️⃣ 安装依赖

```bash
pip install -r requirements.txt
```

### 3️⃣ 配置环境变量

创建 `.env` 文件或直接导出：

```
API_KEY=your_api_key_here
MAX_TEXT_LENGTH=20000
MODEL_PATH=/path/to/your/model
```

### 4️⃣ 运行服务器

如果使用 **FastAPI**：

```bash
uvicorn app.main:app --reload
```

如果使用 **Flask**：

```bash
python app.py
```

---

## 📡 API 使用

### **POST /api/detect**

检测文本是 AI 生成还是人类撰写。

**请求**

```http
POST /api/detect
Content-Type: application/json

{
  "text": "这是一段用于 AI 检测的示例文本。"
}
```

**响应**

```json
{
  "score": 0.87,
  "is_ai_generated": true,
  "analysis": [
    {
      "offset": 35,
      "length": 42,
      "snippet": "此部分可能是 AI 生成的。",
      "confidence": 0.93
    }
  ]
}
```

### **POST /api/suggest** *（可选）*

生成重写建议以提高文本原创性。

**请求**

```http
POST /api/suggest
Content-Type: application/json

{
  "text": "此内容可能听起来像 AI 生成的。"
}
```

**响应**

```json
{
  "suggestions": [
    "考虑添加个人观点或轶事。",
    "使用更多样化的句子结构以增强流畅性。"
  ]
}
```

---

## 💻 客户端示例

```python
import requests

url = "http://localhost:8000/api/detect"
payload = {"text": "AI 生成内容检测至关重要。"}
resp = requests.post(url, json=payload)
data = resp.json()

print("AI 概率:", data["score"])
print("检测到 AI:", data["is_ai_generated"])
for seg in data["analysis"]:
    print(f"片段: {seg['snippet']} (置信度: {seg['confidence']:.2f})")
```

---

## 🧱 部署

* **Docker 支持：** 使用 Docker 轻松部署 Dechecker 的 AI 检测器后端。
* **可扩展性：** 支持异步请求和通过 Kubernetes 或 Docker Compose 进行水平扩展。
* **安全性：** 添加身份验证（API 密钥或 OAuth）以防止滥用。
* **队列处理：** 使用 Celery 或 RabbitMQ 进行批量文本检测任务。
* **监控：** 集成 Prometheus 或 ELK 以实现可观测性。

---

## 🤝 贡献

我们欢迎贡献！
请提交 issue 或 pull request，包含改进、错误修复或功能建议。

提交前：

* 遵循 **PEP8** 编码标准。
* 包含清晰的提交信息。
* 运行测试和代码检查。

---

## 🔗 相关资源

* 🌐 官方网站： [Dechecker](https://dechecker.ai)
* 📘 API 文档：即将推出
* 🧑‍💻 开发者联系： [support@dechecker.ai](mailto:support@dechecker.ai)

